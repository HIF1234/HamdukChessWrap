import type { Metadata, Viewport } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'Hamduk Chess Wrap - Your Year in Chess',
  description: 'Discover your chess journey with AI-powered insights, personalized analytics, and beautiful visualizations. Your year in chess, wrapped.',
  generator: 'v0.app',
  keywords: ['chess', 'chess wrap', 'chess analytics', 'chess.com', 'lichess', 'chess statistics', 'chess AI'],
  authors: [{ name: 'Hamduk Chess' }],
  openGraph: {
    title: 'Hamduk Chess Wrap',
    description: 'Your year in chess, wrapped with AI insights',
    type: 'website',
  },
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export const viewport: Viewport = {
  themeColor: '#00D9FF',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 5,
  userScalable: true,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
